define(['backbone',
        'views/thumbnailView'
    ], function(Backbone, ThumbnailView) {

var App = global.App = {

    init: function(options) {
        console.log(options);
    }

    return App;
});

